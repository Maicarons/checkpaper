# 项目简介

**CheckPaper** 是一个基于 AI 的学术论文验证智能体系统。它能自动检测学术论文中的各种问题，包括格式不规范、引用错误、参考文献造假、数据真实性问题等，帮助研究者提升论文质量和可信度。

## 为什么需要 CheckPaper？

学术论文在发表前会经过严格的审稿流程，但许多常见问题仍然容易被遗漏：

- **格式不一致** — 标题层级错误、图表引用缺失、编号不连续
- **引用问题** — 正文引用与参考文献列表不匹配、重复引用或缺失引用
- **参考文献造假** — 伪造的 DOI、不存在的论文、掠夺性期刊引用
- **数据完整性问题** — 统计数据不一致、无法验证的数据来源、可疑的 p 值

CheckPaper 通过 AI 驱动的分析结合学术数据库验证，全面解决这些挑战。

## 核心架构

```
┌─────────────────────────────────────────────────────┐
│                  React 前端界面                       │
│         (上传 · 验证 · 报告 · 历史记录)                │
└─────────────────────┬───────────────────────────────┘
                      │ HTTP API
┌─────────────────────▼───────────────────────────────┐
│               FastAPI 后端服务                        │
│  ┌──────────┐ ┌────────────┐ ┌──────────────────┐  │
│  │ 文档服务  │ │ 验证服务    │ │ 报告生成器        │  │
│  └─────┬────┘ └─────┬──────┘ └──────────────────┘  │
│        │             │                               │
│  ┌─────▼─────────────▼──────┐                       │
│  │    Agent 服务             │                       │
│  │  (OpenAI Agents SDK)     │                       │
│  └─────────────┬────────────┘                       │
│                │ MCP 协议                            │
│  ┌─────────────▼────────────┐                       │
│  │    MCP 工具服务器         │                       │
│  │  解析 · 检查 · 验证       │                       │
│  └──────────────────────────┘                       │
└─────────────────────────────────────────────────────┘
```

## 核心技术

| 组件 | 技术 |
|------|------|
| 后端 | Python 3.11+ · FastAPI · SQLModel |
| 前端 | React 18 · TypeScript · Ant Design |
| AI Agent | OpenAI Agents SDK · MCP 协议 |
| 文档解析 | PyMuPDF · python-docx · pylatexenc |
| 参考文献验证 | Crossref API · Semantic Scholar API |
| 数据库 | SQLite (开发) · MySQL (生产) |
| 部署 | Docker · Docker Compose |

## 支持的文件格式

| 格式 | 扩展名 | 解析器 |
|------|--------|--------|
| PDF | `.pdf` | PyMuPDF |
| Word | `.docx`, `.doc` | python-docx |
| LaTeX | `.tex`, `.latex` | pylatexenc |
| BibTeX | `.bib` | bibtexparser |

## 验证类型

CheckPaper 支持 **6 种核心验证类型**：

| 类型 | 说明 |
|------|------|
| **格式检查** | 标题层级、编号规范、布局、目录一致性 |
| **图表引用** | 图表引用的完整性和准确性 |
| **参考文献引用** | 正文引用与参考文献列表的一致性 |
| **数据来源** | 数据来源的验证和可访问性 |
| **数据处理** | 统计方法验证、p 值检查 |
| **参考文献验证** | DOI 验证、元数据匹配、造假检测 |

## 下一步

- [快速开始](/zh/guide/getting-started) — 几分钟内安装并运行 CheckPaper
- [功能特性](/zh/guide/features) — 深入了解每项验证功能
- [部署指南](/zh/guide/deployment) — Docker 生产环境部署
